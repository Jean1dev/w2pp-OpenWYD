# Bugs in-game (cliente real ↔ servidor Go) — mapeamento

> Estado: cliente "Cavaleiros de Kersef" 7662 conecta → loga → cria char → entra no mundo → anda.
> Estes são os bugs observados depois disso, com causa-raiz provável e o que falta para corrigir.
> Prioridade: P0 (quebra multiplayer/jogo) → P3 (cosmético).

| # | Bug | Prioridade | Causa-raiz provável | O que falta | Precisa do agente Windows? |
|---|-----|:---:|---|---|:---:|
| B1 | **Player duplica a cada passo** (visto de outra conta) | ~~P0~~ **NÃO REPRODUZIDO / OK no cliente real** | Fluxo básico e cruzamento de visão com 2 usuários testados no cliente real sem duplicação/bug visual. O código já envia `CreateMob` na entrada, `RemoveMob` no logout e revela NPCs/mobs ao andar; não há falha observável hoje. | Manter em observação; não mexer sem caso reproduzível. | — |
| B2 | **Gold inicial negativo** (-858993664) | ~~P1~~ **RESOLVIDO** | O valor `-858993664` = `0xCCCCCC00` = bytes `00 CC CC CC` no **offset 24** do `STRUCT_MOB`. O template BaseMob é um dump de memória com **padding 0xCC não-inicializado**; o cliente lê o gold no offset 24. Zerado em `EncodeCNFCharacterLoginRaw` (offsets 24-31). | — | — |
| B3 | **Sem NPCs/monstros no mapa** | P1 → **PARCIAL** | Spawn nunca ligado. **Feito:** parser `NPCGener.txt` (6103 geradores), loader dos templates `npc/<nome>` (816B), `SpawnMob` (10.496 mobs spawnados, cap 20k), e `CreateMob` dos mobs na visão na entrada **e ao andar** (`revealMobsInView` + grid). **IA iteração 1 feita** (`world/tick.go` + `handler/mobai.go`): monstro agro por proximidade/retaliação, persegue (1 tile/tick) e ataca o player corpo-a-corpo. **IA iteração 2 (parte 1) feita — jul/2026:** **hostilidade por clan** (`world/clan.go` porta `g_pClanTable[9][9]` Basedef.cpp:207; `FindEnemyFromView` porta a janela 9×9 / clan 7-8 16×16 assimétrica de CMob.cpp:1308; templates reais: clan 1/5 agressivos, 2/3/4/… passivos até retaliação — `Lobo` starter é clan 2 e não agride, fiel ao jogo) e **ataque ranged** (`Entity.Range` = max EF_RANGE dos equips do template via `ItemList.Ranges()`+efeitos de instância, port de BASE_GetMobAbility Basedef.cpp:2415; decisão atacar/recuar/perseguir do BattleProcessor CMob.cpp:308-327 com a métrica `g_pDistanceTable` de BASE_GetDistance — arqueiros/magos atacam a ≤4 tiles e **recuam** se o player cola e o roll de Dex falha). **Iteração 2 parte 2 feita — pathfinding real** (`tmserver/internal/route`: port de `BASE_GetRoute`/`BASE_ApplyAttribute`; HeightMap+AttributeMap baked no boot → `handler.Config.Heights`; `mobStep`/`mobRetreat` roteiam e nunca atravessam parede; gate de dormência `wakeRadius`=12 → 10k mobs idle ≈ 0,2-0,3ms/tick). **Iteração 2 parte 3 feita — roaming/patrulha** (parser NPCGener completo Start→Seg[0]/Dest→Seg[4]; `SpawnMobAt` com waypoints randomizados por instância; `mobRoam`+`setSegment` portam StandingByProcessor/SetSegment; âncora de aggro/leash = SegmentX; respawn preserva rota; gate `roamRadius`=20). **Iteração 2 parte 4 feita — respawn por gerador + grupos (ENCERRA o B3)**: `world/generator.go` porta GenerateMob (Server.cpp:3442) no LCG com CurrentNumMob/MaxNumMob; timer 1×/min `min%MG==idx%MG`; grupos Leader/Follower (follower não agride sozinho; bater num membro arrasta o grupo via `setGroupBattle`); boot = RegisterGenerators + 1 GenerateMob/bloco. **Deferido p/ frentes futuras:** mob-vs-mob (guardas), summons/RT5, Formation, chat de mob, EnemyList[13], KEFRA; ~1400 templates de Leader sem arquivo. | — (ver SESSION-PRIMER §7 p/ deferidos); reveal de players ao cruzar a visão andando compartilha com B1 | — |
| B4 | **Preview de classe na seleção = sempre TK** | ~~P2~~ **RESOLVIDO** | `STRUCT_SELCHAR` enviada **sem equipamento** → cliente desenha modelo padrão. | `sel.Equip[slot]` agora vem do `MobEquip` do template BaseMob da classe (`protocol.MobEquip` + `d.selCharsFrom`). | — |
| B5 | **Level mostra +1 na seleção** | ~~P3~~ **RESOLVIDO** | O cliente renderiza `SELCHAR.Score.Level` como one-based na tela de seleção. **Feito:** o preview envia `level-1` só no `SELCHAR`; o level autoritativo em jogo/login/persistência continua inalterado. | — | — |
| B6 | **Posição exata não persiste** | **COMPORTAMENTO ESPERADO** | Confirmado no cliente real/design atual: ao relogar, o personagem nasce no centro/spawn da última cidade visitada, não na coordenada exata onde deslogou. | Não implementar persistência de coordenada exata salvo se a regra de jogo mudar. | — |

## Observações
- **B1** foi mantido apenas como histórico: cliente real com 2 usuários não reproduziu duplicação/bug de visão.
- **B6** não é bug: spawn por última cidade visitada é o comportamento desejado.
- Setup: contas `test`/`test123` e `test2`/`test123`. Stack via `docker compose up`.

| B7 | **NPCs sem funcionalidade** (nome só no hover; loja/troca não abrem) | ~~P1~~ **PARCIAL/RESOLVIDO para loja+banco** | **Feito:** `CreateMob.Score.Merchant`, lojas NPC (`REQShopList`/`Buy`/`Sell`), banco/cargo e compra de item com `Price==0`. Ainda faltam NPCs específicos de quest/combinação/refino. | Implementar demais NPCs data-driven conforme roadmap. | depende do NPC |

| B8 | **Barra de XP não anda / char não upa** | ~~P1~~ **RESOLVIDO** | O `MSG_Attack` era enviado com `HEADER.ID = conn do atacante`. O cliente aplica `Dam[]` aos alvos independente do header (por isso o ataque do mob fere o player), mas só aplica os campos do **próprio atacante** (`CurrentExp`/`CurrentHp`/`CurrentMp` = barra de XP) quando o pacote chega como evento de cena, i.e. `HEADER.ID = ESCENE_FIELD` (30000). O servidor contava o exp certo (templates têm `Exp@32`, `grantExp`/curva OK, persistência OK) — só não chegava na barra. | — (broadcast do attack agora vai com `ID=protocol.IDScene` p/ atacante + in-view, `handler/combat.go`; teste `TestAttackHeaderIsSceneField`; fonte `_MSG_Attack.cpp:25`) | — |

| B9 | **Personagem morto continua atacando/consumindo itens (HP=0)** | ~~P1~~ **RESOLVIDO** | Mesmo grupo do B8: o ataque do MOB ia com `HEADER.ID = id do mob`, não `ESCENE_FIELD`. O cliente aplica o `Dam[]` (player toma dano), mas só coloca a **própria vítima** no estado "morto" quando o golpe chega como evento de cena. Sem isso o cliente nunca entrava em morte → continuava agindo (ataque + auto-poção, que é client-local). | — (ataque do mob agora vai com `ID=ESCENE_FIELD`, `handler/mobai.go`; guarda do attack endurecida p/ `HP<=0`, `handler/combat.go`; o original faz `GetAttack`→`sm->ID=ESCENE_FIELD` em `GetFunc.cpp` e `SendHpMode`+`AddCrackError` no morto; teste `TestMobAttackHeaderIsSceneField`) | — |

| B10 | **Level-up não concede pontos de atributo** | ~~P1~~ **RESOLVIDO** | `ScoreBonus` (pontos livres) **não fica no `STRUCT_SCORE`/`MSG_UpdateScore`** — fica no `MSG_UpdateEtc` (`SendEtc`, `Basedef.h`). No level-up mandávamos só `UpdateScore`+`Motion`, então o cliente nunca via os pontos novos. Bônus: `EncodeUpdateEtcCoin` (só gold) zerava `ScoreBonus`/`Exp` no cliente a cada compra/teleporte. | — (novo `EncodeUpdateEtc` completo + `sendEtc`; enviado no level-up (`grantExp`) e em todos os refreshes de gold (shop/cargo/teleporte/restart/loot); testes `TestUpdateEtcLayout`; fonte `SendFunc.cpp:SendEtc`) | — |

| B11 | **Não consegue comprar itens** (todas as compras falham em silêncio) | ~~P1~~ **RESOLVIDO** | O cliente manda `_MSG_Buy` com o slot de destino (`MyInvenPos`) que ELE acha vazio, mas o servidor tinha um item nesse slot (no caso visto, índice 21 = `BeastMaster`, item de modelo de classe que o cliente não desenha na mochila → o slot aparenta vazio). O Go só dava `return` mudo na condição "destino ocupado", então o cliente nunca aprendia que o slot estava ocupado e **reenviava o mesmo `myPos` infinitamente** → nenhuma compra acontecia. Diagnóstico via log `buy reject ... myPos=10 destItem=21`. | — (na condição destino-ocupado o `buy` agora reenvia `MSG_SendItem` daquele slot p/ re-sincronizar o cliente, igual ao original `_MSG_Buy.cpp:158-162`; teste `TestBuyOccupiedSlotResync`, `handler/shop.go`) | — |

| B12 | **Player não tira dano do mob** (mob fere o player, o contrário não) | ~~P0~~ **RESOLVIDO** | Regressão introduzida pela frente de skills (M3): o `attack` passou a **confiar em campos UNVERIFIED do cliente como gate**. Com o catálogo de skills carregado (config de produção), (a) `SkillIndex ∈ [0,248)` ativava a validação de skill e o **ataque inteiro era derrubado** no learn-mask/class-gate (melee do cliente 12000 pode chegar com `SkillIndex=0`); (b) `Dam[i].Damage` fora dos sentinelas `-2/-1/0` era **zerado + crack** (o cliente pode mandar o dano calculado localmente). O mob→player não passa por esse caminho (ataque gerado no servidor), por isso só um lado quebrou. Agravante: os testes de handler rodavam **sem `Spells`** no Config, então o caminho quebrado nunca era exercitado. | — (fix: o caminho de skill só engaja com **intenção explícita** — alguma `Dam` com sentinela `-1`; claims inesperados resolvem como melee com log debug, nunca zeram — o valor é ignorado mesmo, dano é server-authoritative. Regression: `TestMeleeAlwaysDamagesMob` (`combat_regression_test.go`) cobre as 4 codificações plausíveis de melee CONTRA UM MOB e com o catálogo carregado; `handler/combat.go`) | captura do melee real (SkillIndex/Dam) pedida em `agent-prompt-skills.md` |

| B13 | **Proteção Elemental (BM 53) não protege contra Fogo** (issue #233) | ~~P2~~ **RESOLVIDO** | O buff **funcionava** — o print da issue (BM Nv 400, Elemental 255/255) mostra `Sagrado 27 / Trovão 27 / Fogo 0 / Gelo 27`, e `27` é exatamente a fórmula do affect 25: `add = (Value + Level/4)/10 = (10+63)/10 = 7`, mais `+20` do teto `Level >= 255`. O errado era **em quais índices** ele caía. O legado (`Basedef.cpp:4239`) soma em locais chamados `Fogo/Trovao/Gelo`, mas os locais estão **trocados na origem** (`Basedef.cpp:3919`: `Sagrado←Resist[0]`, `Trovao←[1]`, `Fogo←[2]`, `Gelo←[3]`) → os índices realmente tocados são `1/2/3`, ou seja **buffa Sagrado e pula o Fogo**. A ordem real vem do conteúdo: `Orb_de_Fogo`=`EF_RESIST1`, `Orb_de_Gelo`=`EF_RESIST2`, `Orb_Sagrada`=`EF_RESIST3`, `Orb_de_Relâmpago`=`EF_RESIST4` (`ItemList.csv:1013-1019`) e `EF_RESIST1..4`→`Resist[0..3]` (`CMob.cpp:640-643`). O port Go tinha copiado o legado fielmente (`for k := 1; k < 4`). | — (**divergência deliberada**, como o ÷8 da #92: `affect_score.go` case 25 agora soma em `{0,1,3}` = Fogo/Gelo/Trovão e pula Sagrado. No mesmo passe fechou o clamp final `0..100` de base+buff que o legado faz em `Basedef.cpp:4737-4767` e o Go só aplicava à parte de equipamento — sem ele o `int8` do wire estoura e vira resist negativo no cliente: novo `effectiveResist` reusado por `computeScore` e `resolveSkillHit`. Testes: `TestElementalProtectionResists`, `TestEffectiveResistClamps`, `TestComputeScoreClampsResist`, `TestSkillResistMitigationClampedAtCeiling`) | — |

| B14 | **Duração das habilidades alta demais** (issues #92, #202, #229 — o mesmo relato três vezes) | ~~P2~~ **RESOLVIDO** | As duas primeiras correções erraram o alvo. A duração é `(AffectTime+1)*(100+Special)/100` ticks de 8 s (`world/affect.go`, port fiel de `Server.cpp:9209`), e **todas** as linhas de buff real do `SkillData.csv` têm `AffectTime` bruto **600** — ou seja a base é constante e quem domina é o multiplicador de mastery `(100+Special)/100`, que vale **até 5×** no teto de `effectiveSpecial`. A #92 dobrou o divisor do loader (÷4→÷8): mexeu só na base, levando o teto de ~100 min para ~51 min, e o print da #229 (Foema Nv 400) ainda mostrava 33-38 min — conferindo pela fórmula, `Special ≈ 226-275`. Pior, o divisor é aplicado no **load com divisão inteira**, então custou à cauda curta da tabela muito mais que a metade pretendida (bruto 30 → `30/4=7` virou `30/8=3`, −57%), que é exatamente o que a #236 reportou. A #202 era outra coisa: bug de exibição do timer da Divina. | — (**divergência deliberada**, mesma linha do case 25 do B13: o loader volta ao ÷4 legado e o tuning passa a viver num único lugar, `world.AffectDuration`, aplicado quando um **cast** instala o affect — `ScalePct` 15 / piso 60 s / teto 10 min, configuráveis por `-affect-scale-pct`, `-affect-min-seconds`, `-affect-max-minutes`. O piso vale só para affects **não agressivos**, senão alongaria debuffs curtos como `Enfraquecer` (bruto 5). Os casos especiais legados (clamps de tipo 1/3/10, sentinela "infinita") não são tunados. Affects de item/cash/montaria/Divina escrevem `Entity.Affect` direto e não passam por aqui — os ícones 7d/29d/1h do print seguem intactos. Curva resultante: 2m56 sem mastery → teto de 10 min. Testes: `TestAffectDurationPolicy`, `TestAffectDurationSkipsLegacyClamps`, `TestCastBuffDurationCurve`, `TestCastBuffDurationLeavesItemAffects`) | — |

| B15 | **Escudo Dourado dura pouco demais** (issue #236) | ~~P2~~ **RESOLVIDO** | A #236 foi reportada contra o ÷8 da #92 e o B14 a tratou como já explicada por ele — mas o mesmo passe que restaurou o ÷4 introduziu o `ScalePct` de 15 %, e ele **reproduziu o sintoma pela outra ponta**. O scale foi calibrado nas linhas longas (bruto 600 → 150 ticks); aplicado à cauda curta, `(7+1)*15/100 = 1` tick, e o piso de 60 s achatava a skill 85 em **64 s fixos em qualquer mastery**. Para quem tinha mastery investida isso é **mais curto do que quando a issue foi aberta** (com o ÷8: 32 s sem mastery, mas 104 s em `Special ≈ 226` e 160 s no teto) e 5× mais curto que o legado. Não era só a 85: 41 Teleporte, 90 Toxina da Serpente, 95 Invisibilidade, 96 Poder Superior, 47 Cancelamento, 216 Magia Misteriosa, 219 Thunder Lising e 246 Bleeding caíam todas no mesmo 64 s. | — (**divergência deliberada**, refinamento do B14: `AffectDuration.withinTargetBand` isenta do `ScalePct` o affect **não agressivo** cuja duração legada já cabe sob `MaxTicks` — não há o que cortar em quem já está dentro da banda alvo. As linhas longas ficam **byte-a-byte** iguais (2m56 sem mastery → teto de 10 min, a #229 não regride) e a cauda curta volta à curva legada com o piso de 60 s por baixo: skill 85 = 64 s → 2m08 em `Special` 100 → 5m20 no teto. Affects **agressivos** ficam fora da isenção de propósito — alongar CC (`Perseguição`, `Nevasca`, `Lâmina Congelante`, `Conexão de Gelo`, todos bruto 1-3) seria mudança de PvP que ninguém pediu. A pertinência à banda é julgada pela duração **base** (`AffectTime+1`, sem mastery), nunca pelo tick já inflado por `Special`: assim ela é propriedade da linha do CSV e constante em toda a curva. Julgando o valor inflado — como saiu o primeiro corte deste fix — o `Teleporte` (base 16 ticks) cruzava `MaxTicks` no alto da mastery e caía do ramo identidade para o de 15 %, indo a 2m08 sem mastery, 6m24 em `Special` 200 e **de volta a 1m36** em `Special` 400: um buff que piora quanto mais se investe nele. Testes: `TestAffectDurationPolicy`, `TestCastBuffDurationCurve` (skills 85 e 90) e, sobre o **CSV real**, `TestReleaseAffectDurationIsMonotonicInMastery` / `StaysInBand` / `Golden` — o seam que faltava: a #92 mexeu no loader e a #229 na política, cada uma testada contra fixtures, e nenhuma rodou sobre a tabela inteira, que é como a cauda curta passou batido duas vezes) | — |

## (adicione aqui os bugs que você já viu)
- …
